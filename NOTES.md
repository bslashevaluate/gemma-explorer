# User Preferences

- The user prefers to use the conda environment named `gpt2i`.
- Commands that require dependencies (like Python or Node.js/npm) should be run within this environment (e.g., using `conda run -n gpt2i <command>`).
